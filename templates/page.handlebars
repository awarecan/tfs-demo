// ------------------------------------------------------------------------------
//      This code was generated by handlebars/grunt.
//
//      Changes to this file may cause incorrect behavior and will be lost if
//      the code is regenerated.
// ------------------------------------------------------------------------------

var controls = require('./../controls/controls.js');
var utilities = require('./../utils/utilities.js');
var pagedata = utilities.readJsonFile(__dirname + '/../pages/{{PageName}}Page.json');

function {{PageName}}Page(){
{{#if PageUrl}}
      this.pageUrl = '{{PageUrl}}';
{{/if}}
{{#if Controls}}
  {{#each Controls}}
      this.{{@key}}{{Type}} = new controls.{{Type}}(pagedata.Controls.{{@key}}.SelectType, pagedata.Controls.{{@key}}.Selector);
  {{/each}}
{{/if}}
{{#if Tables}}
  {{#each Tables}}
      this.{{@key}}Table = new controls.Table(pagedata.Tables.{{@key}}.RowRepeater, pagedata.Tables.{{@key}}.ColRepeater, pagedata.Tables.{{@key}}.Columns);
  {{/each}}
{{/if}}
{{#if Repeats}}
  {{#each Repeats}}
      this.{{@key}}Repeat = new controls.Repeat(pagedata.Repeats.{{@key}}.Selector,pagedata.Repeats.{{@key}}.Columns);
  {{/each}}
{{/if}}
};

module.exports = {{PageName}}Page;
